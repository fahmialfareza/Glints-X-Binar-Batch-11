// Boolean only has 2 value, true and false
const isTodayRaining = true;

if (isTodayRaining) {
  // If today is raining
  console.log("Use umbrella!");
} else {
  // Else
  console.log("Just bring umbrella not use it!");
}
