// Boolean only has 2 value, true an false
let isTodayRaining = true;

if (isTodayRaining == true) {
  // If isTodayRaining
  console.log("Use umbrella!");
} else {
  // If today is not raining
  console.log("Just bring umbrella not use it!");
}
